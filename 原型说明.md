# 审核模块 PC 端原型说明

> 版本：v0.1  
> 日期：2026-07-23  
> 对应 PRD：Sansight审核模块PC端需求PRD_v0.1.md

---

## 一、原型文件说明

| 文件 | 说明 |
| --- | --- |
| `index.html` | 交互式原型（单页应用），直接浏览器打开即可使用 |
| 交互方式 | 鼠标点击、弹窗操作、面板切换 |

**使用方式：** 双击 `index.html` 在浏览器中打开，原型模拟 PC 端审核模块的全部核心交互。

---

## 二、原型覆盖的页面

### 2.1 审核事件列表页（默认首页）

**覆盖的 PRD 章节：** 第 4 章（4.1-4.8）

| 功能区域 | 交互说明 | 对应 PRD |
| --- | --- | --- |
| 侧边栏导航 | 「审核中心」高亮选中状态；其他菜单项占位 | 3.3 |
| 事件大类 Tab | 5 个 Tab 可点击切换；数字徽章展示待处理数 | 4.2 |
| 筛选栏 | 搜索框、下拉筛选（占位）、日期范围 | 4.3 |
| 展示模式切换 | 「事件明细」/「按设备分组」切换按钮 | 4.5 |
| 设置入口 | ⚙ 按钮点击进入审核设置页 | 7.1 |
| 统计摘要栏 | 预警总数 + 待处理/已处理/已忽略数量 | 4.6 |
| 表格数据 | 11 列完整展示，10 条样本数据 | 4.4 |
| 操作按钮 | 详情 → 跳转详情页；确认处理/忽略 → 弹出弹窗 | 4.4 |
| 多选 + 批量操作 | 勾选多行 → 批量操作栏出现 | 4.7 |
| 全选 | 表头全选 checkbox 控制所有可选行 | 4.7 |
| 分页 | 页码、每页条数选择器 | 4.8 |

### 2.2 审核事件详情页

**覆盖的 PRD 章节：** 第 5 章（5.1-5.3）

| 功能区域 | 交互说明 | 对应 PRD |
| --- | --- | --- |
| 返回按钮 | 点击返回审核列表 | 5.1 |
| 左面板（设备信息） | 设备图片、编号、类型、品牌、状态、项目、物模型 | 5.2 |
| 右面板 Tab 切换 | 「事件信息」/「操作记录」两个 Tab 可切换 | 5.3 |
| 事件信息 Tab | 事件类型、发生时间、内容、来源、差异信息区 | 5.3 |
| 操作记录 Tab | 操作留痕列表（当前样本数据操作为空） | 5.3 |
| 底部操作栏 | 确认处理 / 忽略 / 服务召请 / 查看动态曲线 | 5.4 |
| 关联任务按钮 | 点击打开右侧任务面板 | 6.1 |
| 确认处理弹窗 | 处理结果填选 + 快捷短语标签（可点击选中） | 5.4 |
| 忽略弹窗 | 忽略原因填选 + 快捷短语标签 | 5.4 |
| 状态标签 | 详情头部展示当前状态 Tag（待处理/已处理/已忽略） | 5.1 |

### 2.3 关联任务面板

**覆盖的 PRD 章节：** 第 6 章（6.1-6.4）

| 功能区域 | 交互说明 | 对应 PRD |
| --- | --- | --- |
| 右侧滑出面板 | 从右侧滑入，展示关联任务列表 | 6.2 |
| 关闭按钮 | 点击关闭面板 | — |
| 创建任务按钮 | 点击弹出创建任务浮窗（演示占位） | 6.3 |

### 2.4 审核设置页

**覆盖的 PRD 章节：** 第 7 章（7.1-7.4）

| 功能区域 | 交互说明 | 对应 PRD |
| --- | --- | --- |
| 手风琴分组 | 5 个事件大类可展开/收起，显示已开启数/总数 | 7.2 |
| 事件类型开关 | 每个事件类型有 Toggle 开关（默认开启） | 7.3 |
| 授权人员文案 | 展示"授权人员：全部用户"文案 | 7.4 |
| 返回按钮 | 点击返回审核列表 | — |

---

## 三、页面流转路径

```
侧边栏「审核中心」
  → 审核事件列表（默认）              ← 见 2.1
    ├── 点击表格「详情」→ 审核事件详情页 ← 见 2.2
    │   ├── 点击「确认处理」→ 弹窗 → 提交
    │   ├── 点击「忽略」→ 弹窗 → 提交
    │   ├── 点击「关联任务」→ 侧面板   ← 见 2.3
    │   └── 点击「返回」→ 列表页
    ├── 点击「⚙ 设置」→ 审核设置页     ← 见 2.4
    │   └── 点击「返回」→ 列表页
    ├── 勾选多行 → 批量操作栏 → 批量处理
    ├── 点击大类 Tab → 切换大类数据
    └── 切换展示模式 → 事件明细/按设备分组
```

---

## 四、设计原则

1. **布局遵循 MySANY 现有规范**：左导航 + 顶部栏 + 内容区，颜色和间距与现有 Web 统一
2. **表格列对齐 PRD 第 4.4 章**：11 列 + 列宽预设 + 兜底规则
3. **设备主标识兜底规则**：车牌号 → 设备昵称 → 设备序列号 → 名牌编号
4. **处理状态标签化**：待处理（橙色 Tag）/ 已处理（绿色 Tag）/ 已忽略（灰色 Tag）
5. **弹窗设计**：快捷短语标签可点击选中，自动填入文本框
6. **原型使用样本数据**：数据来源于 MySANY 测试环境真实预警记录脱敏

---

## 五、未覆盖的功能（后续迭代）

| 功能 | 说明 | 对应 PRD 章节 |
| --- | --- | --- |
| 按设备分组模式 | 点击模式切换后仅弹出提示 | 4.5 |
| 预警趋势图 | 列表中仅展示占位文案 | 4.6 |
| 设备下钻列表 | 从设备分组模式进入，本期简化 | 4.5 |
| 动态曲线内嵌 | 详情页操作按钮仅弹出提示 | 5.5 |
| 检查结果弹窗 | 详情页操作按钮仅弹出提示 | 5.5 |
| 服务召请页 | 跳转已有 Web 页面，未覆盖 | — |
| 通用任务详情页 | 点击关联任务 → 跳转通用任务详情 | — |
